import styled from "styled-components";

export const TxHeader = styled.h1`
  font-size: 60pt;
  font-weight: ExtraBold;
  color: var(--white);
`;

export const TxSearch = styled.h3`
  font-size: 24pt;
  font-weight: light;
  color: var(--grey);
`;

export const TxSearchBtn = styled.h3`
  font-size: 24pt;
  font-weight: semiBold;
  color: var(--grey);
`;

export const TxSmallHeader = styled.h2`
  font-size: 24pt;
  color: var(--dark-gray);
`;

export const TxFilterOptions = styled.p`
  font-size: 18pt;
  font-weight: light;
  color: var(--grey);
`;

export const TxOff = styled.p`
  font-size: 18pt;
  font-weight: light;
  color: var(--light-grey);
`;
